#include <stdio.h>
int main()
{

    // Comments: Comments are used to make the code more readable and understandable. They are ignored by the compiler and do not affect the execution of the program.

    // Types of comments:
    // 1. Single-line comments: Use // at the end of a line.
    // 2. Multi-line comments: Use /* and */

    return 0;
}